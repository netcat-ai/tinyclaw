apiVersion: v1
kind: Service
metadata:
  name: clawman
  namespace: claw
  labels:
    app: clawman
spec:
  selector:
    app: clawman
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP
